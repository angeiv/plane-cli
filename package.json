{
  "name": "plane-cli",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "type": "module",
  "files": [
    "dist/src",
    "LICENSE",
    "README.md"
  ],
  "bin": {
    "plane": "./dist/src/bin/plane.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx src/bin/plane.ts",
    "lint": "biome check src tests",
    "format": "biome check --write src tests",
    "release:pack": "node scripts/prepare-release.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "commander": "^14.0.1",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.13",
    "@types/node": "^22.0.0",
    "tsx": "^4.22.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
